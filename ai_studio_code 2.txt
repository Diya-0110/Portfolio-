"use client";
import { motion } from "framer-motion";
import { Mail, Linkedin, Instagram, Youtube, Twitter } from "lucide-react";

const socials = [
  { name: "LinkedIn", icon: Linkedin, href: "#" },
  { name: "Instagram", icon: Instagram, href: "#" },
  { name: "YouTube", icon: Youtube, href: "#" },
  { name: "Twitter", icon: Twitter, href: "#" },
];

export default function Contact() {
  return (
    <div className="pt-40 pb-20 min-h-screen flex flex-col justify-center">
      <div className="container mx-auto px-8">
        <div className="max-w-4xl">
          <motion.h1 
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            className="text-6xl lg:text-9xl font-bold tracking-tightest leading-[0.8] mb-12"
          >
            Let&apos;s build <br /> <span className="text-zinc-800">the future.</span>
          </motion.h1>

          <div className="grid md:grid-cols-2 gap-20 items-end">
            <div>
              <p className="text-2xl text-zinc-400 leading-relaxed mb-12">
                Currently open to select collaborations with brands and founders looking to push the boundaries of storytelling.
              </p>
              
              <a 
                href="mailto:hello@diyalalwani.com"
                className="text-3xl lg:text-5xl font-bold tracking-tight hover:text-blue-500 transition-colors border-b-2 border-white/10 pb-4 block"
              >
                hello@diyalalwani.com
              </a>
            </div>

            <div className="flex flex-wrap gap-8">
              {socials.map((social) => (
                <a 
                  key={social.name} 
                  href={social.href}
                  className="flex flex-col gap-4 group"
                >
                  <social.icon className="w-6 h-6 text-zinc-500 group-hover:text-white transition-colors" />
                  <span className="text-[10px] font-mono text-zinc-600 uppercase tracking-widest">{social.name}</span>
                </a>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}